<h1>Browse questions from your peers ~</h1>

<div class="row">
  {{!-- <div class="col-sm-2">

  </div>
  <div class="col-sm-10"> --}}
    {{#each model as |question|}}
      {{#link-to 'question' question.id}}
        {{question-tile question=question}}
      {{/link-to}}
    {{/each}}
  {{!-- </div> --}}
</div>
